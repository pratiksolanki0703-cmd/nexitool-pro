// ============================================================================
// ANNOUNCEMENT BANNER — edit this file to show/hide a message bar at the very
// top of every page (above the header). No other file needs to change.
//
//   enabled: true   -> shows the banner (if message is not empty)
//   enabled: false  -> hides it completely
//
//   message: the text to show. Leave as "" to hide even if enabled is true.
//
// Styling (all optional, any valid CSS value):
//   backgroundColor, textColor, fontSize
//
// Size (height/padding) is fixed in js/announcement-banner.js so the layout
// never breaks — only edit the values below.
// ============================================================================
window.ANNOUNCEMENT_CONFIG = {
    enabled: false,
    message: "",
    backgroundColor: "#f5b301",
    textColor: "#111111",
    fontSize: "14px"
};
