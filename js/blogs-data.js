// Blog posts config — same pattern as tools-data.js.
// tags should include relevant tool ids so Related Tools/Related Blogs auto-matching works.
//
// Fields: title, slug, description, date, author, image, tags (array of tool ids), path

const BLOGS = [];
